const Profile = ({name, tag, location, image, stats}) => {
  console.log(props)
    return (<>
      <p>{name}</p>;
      <p>{tag}</p>;
      <p>{location}</p>;
      <p>{image}</p>;
      <p>{stats}</p>;
    </>
    )

}

export default Profile
